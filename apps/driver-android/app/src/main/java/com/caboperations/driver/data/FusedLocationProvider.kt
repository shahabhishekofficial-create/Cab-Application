package com.caboperations.driver.data

/** Backward-compatible import shim; session start uses the canonical location provider. */
typealias FusedLocationProvider = com.caboperations.driver.location.FusedLocationProvider
